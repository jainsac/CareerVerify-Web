import crypto from "crypto"; import {prisma} from "./prisma"; export async function generateUniqueCareerId(){for(let i=0;i<10;i++){const x=crypto.randomBytes(7).toString("hex").toUpperCase();const id="CV-IND-"+x.slice(0,4)+"-"+x.slice(4,8)+"-"+x.slice(8,12);if(!(await prisma.careerProfile.findUnique({where:{careerId:id}})))return id}throw new Error("Unable to generate unique Career ID")}