import {NextResponse} from "next/server"; import {prisma} from "../../../../lib/prisma"; import {hashPassword,signSession,sessionCookie} from "../../../../lib/auth"; import {generateUniqueCareerId} from "../../../../lib/career-id"; export async function POST(req:Request){try{const b=await req.json();const name=String(b.name??"").trim(),email=String(b.email??"").trim().toLowerCase(),password=String(b.password??""),role=b.role==="EMPLOYER"?"EMPLOYER":"EMPLOYEE";if(name.length<2||!email.includes("@")||password.length<8)return NextResponse.json({error:"Valid name, email and password of at least 8 characters are required"},{status:400});if(await prisma.user.findUnique({where:{email}}))return NextResponse.json({error:"Email already registered"},{status:409});const careerId=role==="EMPLOYEE"?await generateUniqueCareerId():undefined;const user=await prisma.user.create({data:{name,email,passwordHash:await hashPassword(password),role,careerProfile:careerId?{create:{careerId}}:undefined}});const res=NextResponse.json({ok:true,userId:user.id,careerId});res.cookies.set(sessionCookie,signSession(user.id),{httpOnly:true,sameSite:"lax",secure:process.env.NODE_ENV==="production",path:"/",maxAge:604800});return res}catch{return NextResponse.json({error:"Registration failed"},{status:500})}}