export default async function PublicProfile({params}:{params:Promise<{token:string}>}){const {token}=await params; return <div className="wrap"><div className="form card"><div className="pill">VERIFIED REFERENCE</div><h1>Career verification</h1><p className="muted">Opaque reference: {token}</p><p>Only explicitly authorized verification information should appear here. Private identity and employment data are not public.</p></div></div>}