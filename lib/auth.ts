import crypto from "crypto"; import bcrypt from "bcryptjs"; import {cookies} from "next/headers"; import {prisma} from "./prisma"; const COOKIE="careerverify_session"; const secret=()=>{const s=process.env.AUTH_SECRET;if(!s||s.length<32) throw new Error("AUTH_SECRET must contain at least 32 characters");return s}; export async function hashPassword(p:string){return bcrypt.hash(p,12)} export async function verifyPassword(p:string,h:string){return bcrypt.compare(p,h)} export function signSession(userId:string){const body=userId+"."+Date.now();const sig=crypto.createHmac("sha256",secret()).update(body).digest("hex");return body+"."+sig} export function verifySession(v:string){const parts=v.split(".");if(parts.length!==3)return null;const [id,ts,sig]=parts;const expected=crypto.createHmac("sha256",secret()).update(id+"."+ts).digest("hex");if(!crypto.timingSafeEqual(Buffer.from(sig),Buffer.from(expected)))return null;if(Date.now()-Number(ts)>7*24*60*60*1000)return null;return id} export async function currentUser(){const c=await cookies();const raw=c.get(COOKIE)?.value;if(!raw)return null;const id=verifySession(raw);return id?prisma.user.findUnique({where:{id},include:{careerProfile:true}}):null} export const sessionCookie=COOKIE;