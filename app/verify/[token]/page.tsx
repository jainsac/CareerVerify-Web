export default async function VerifyToken({params}:{params:Promise<{token:string}>}){const {token}=await params; return <div className="wrap"><div className="form card"><div className="pill">SECURE VERIFICATION</div><h1>Verification request</h1><p className="muted">Reference: {token}</p><p>Consent and verification details are handled through the authorized CareerVerify workflow.</p></div></div>}